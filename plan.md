# Web Samgukgi (가칭) - 게임 기획서

이 문서는 삼국지5 스타일의 웹 기반 턴제 전략 게임 개발을 위한 기획서입니다.
앞으로 새로운 기능을 추가하기 전, 반드시 이 문서의 하단에 기획 내용을 먼저 작성합니다.

## 1. 게임 개요
- **목표**: 삼국지5의 핵심 재미 요소(명성, 진형, 간결한 내정)를 살린 웹 기반 전략 게임
- **플랫폼**: 웹 브라우저 (PC 및 모바일 반응형 지원)
- **주요 특징**: 
  - 직관적이고 깔끔한 UI
  - 복잡한 조작 없이 클릭만으로 진행되는 턴제 시스템

## 2. 핵심 시스템
### 2.1. 명성 시스템
- 군주의 '명성' 수치에 따라 턴마다 내릴 수 있는 명령의 횟수(명령서)가 결정됩니다.
- 전투 승리, 순찰, 이벤트 등을 통해 명성을 높일 수 있습니다.

### 2.2. 내정 시스템
- 1달 단위의 턴제로 진행됩니다.
- **인사**: 장수 등용, 포상, 해고
- **군사**: 훈련, 징병, 출진
- **외교**: 동맹, 파기
- **개발**: 상업, 농업, 치수 등의 도시 발전도 관리

### 2.3. 전투 시스템
- 12가지 진형(추행, 학익, 어린 등)을 활용한 상성 기반 전투
- 각 무장의 무력과 지력, 특기가 전투의 승패를 가릅니다.

## 3. 데이터 구조 (예정)
- **장수 스탯**: 무력(전투력), 지력(전술/조언), 정치(내정), 매력(인사)
- **도시 데이터**: 금, 군량, 인구, 방어력 등

---
*(이 아래부터는 기능 구현 시 순차적으로 기획이 추가됩니다.)*

## [기능 구현 1] 웹 게임 기본 뼈대 구축
### 목표
- 웹 브라우저에서 게임을 실행하기 위한 가장 기초적인 파일들을 준비합니다.
- 화면은 심플하고 깔끔한 다크 테마(어두운 배경)를 적용하여 몰입하기 좋게 만듭니다.

### 파일 구성 계획
- `index.html`: 게임 화면의 구조를 담당하는 뼈대 파일입니다.
- `style.css`: 글자 크기, 색상, 배치 등 화면을 깔끔하게 꾸미는 디자인 파일입니다.
- `script.js`: 사용자가 버튼을 클릭했을 때 반응하게 해주는 동작 파일입니다.

---

## [기능 구현 2] 장수 및 군주 데이터 구조 생성
### 목표
- 컴퓨터가 게임 속 인물들(군주와 장수)의 능력치를 기억할 수 있도록 기초 데이터를 만듭니다.
- 만들어진 군주의 정보를 게임 화면 상단에 자동으로 표시합니다.

### 작업 내역
- `script.js` 파일에 다음 데이터를 기록합니다:
  - 장수 3명 정도의 샘플 데이터 (이름, 무력, 지력, 정치, 매력)
  - 플레이어가 조종할 군주 설정 (예: 조조)
  - 군주의 현재 명성과 남은 행동력(명령서) 상태
- `index.html`과 `script.js`를 수정하여, 게임이 켜질 때 군주의 이름과 명성이 화면에 짜잔! 하고 나타나게 연결합니다.

---

## [기능 구현 3] 시간 흐름(턴 종료) 및 명령서 회복 시스템
### 목표
- 사용자가 '턴 종료' 버튼을 누르면 다음 달로 넘어가게 만듭니다.
- 매달 초가 되면 군주의 명성에 비례하여 명령서를 새롭게 채워줍니다. (명성이 높을수록 행동력이 많아짐)

### 작업 내역
- `script.js` 파일에 현재 게임 시간(연도, 월)을 기억하는 데이터를 추가합니다 (예: 190년 1월 시작).
- '턴 종료' 버튼을 클릭하면 다음과 같은 일들이 일어나는 로직을 작성합니다:
  1. 달(월) 숫자가 1 늘어납니다. 만약 12월에서 턴을 종료하면 연도가 1 늘어나고 1월로 바뀝니다.
  2. 다음 달이 되었으므로, "기본 제공 3개 + (명성에 비례한 추가 개수)" 만큼의 새로운 명령서를 지급합니다.
  3. 바뀐 연도/월, 그리고 다시 채워진 명령서 개수를 화면에 바로 업데이트해서 보여줍니다.

---

## [기능 구현 4] 성(Castle) 이미지 생성 및 화면 배치
### 목표
- 플레이어의 본거지(거성)를 시각적으로 보여주기 위해 중앙 화면에 성 이미지를 추가합니다.
- 게임의 다크 테마와 잘 어울리는 고전적이면서도 깔끔한 성의 모습을 보여줍니다.

### 작업 내역
- AI 이미지 생성 도구를 사용하여 동양풍(혹은 삼국지 느낌)의 멋진 성 이미지를 만듭니다.
- `index.html`의 중앙 메인 화면(`<main>`) 영역에 만들어진 성 이미지를 배치합니다.
- `style.css`를 수정하여 이미지가 화면 크기에 맞게 깔끔하게 조절되도록 만듭니다.
---

## [기능 구현 4] 내정(개발) 시스템 및 도시 데이터 구축
### 목표
- 플레이어가 지배하는 도시의 기초 데이터를 정의하고 관리합니다.
- '내정' 버튼을 누르면 세부 내정 명령(상업, 농업, 치수)을 내릴 수 있는 메뉴가 화면에 나타납니다.
- 각 내정 명령을 실행하면 명령서가 소모되며 도시의 개발도가 향상됩니다.

### 작업 내역
1. **도시 데이터 모델 정의 (`script.js`)**:
   - 플레이어의 거점 도시(예: 이름 '허창', 농업 50, 상업 50, 치수 50, 금 1000, 군량 5000) 데이터를 객체로 정의합니다.
2. **내정 메뉴 UI 구현 (`index.html` / `script.js` / `style.css`)**:
   - '내정' 버튼을 클릭하면 메인 화면 영역(HTML의 `.main-content`)이 내정 메뉴로 전환됩니다.
   - 내정 메뉴에는 **[상업 개발]**, **[농업 개발]**, **[치수 개발]**, **[돌아가기]** 버튼이 생성됩니다.
3. **내정 기능 로직 작성 (`script.js`)**:
   - 각 개발 버튼 클릭 시:
     - 명령서가 남아있는지 확인합니다 (0개보다 많아야 함).
     - 명령서가 있으면 명령서를 1개 소모하고, 해당 개발도를 무작위 수치(예: 5~15 사이)만큼 상승시킵니다.
     - 상승된 도시 상태와 실행 결과를 메인 화면에 텍스트로 보여주고 상단 상태창을 갱신합니다.
     - 명령서가 부족하면 실행하지 않고 "명령서가 부족합니다!"라는 경고 메시지를 메인 화면에 띄웁니다.
   - **[돌아가기]** 버튼 클릭 시:
     - 초기 안내 문구("환영합니다! 천하통일의 위업을 달성하세요.")가 들어있는 기본 메인 화면 상태로 되돌립니다.

---

## [기능 구현 5] 군주 초상화(얼굴) 이미지 추가 및 UI 개선
### 목표
- 플레이어 군주(조조)의 얼굴 이미지를 생성하여 화면 왼쪽 상단에 매끄럽게 배치합니다.
- 초상화가 추가되면서도 레이아웃이 흐트러지지 않도록 다크 테마 디자인을 더욱 고급스럽고 깔끔하게 개선합니다.

### 작업 내역
1. **군주 초상화 리소스 준비 (`ruler_jojo.png`)**:
   - AI 이미지 생성기를 이용하여 삼국지 감성을 담은 군주 조조의 아바타 초상화 이미지를 생성하고 저장합니다.
2. **HTML 구조 수정 (`index.html`)**:
   - 상단 헤더 또는 상태창 옆에 초상화 이미지가 들어갈 공간을 확보하고 `<img id="ruler-portrait" src="ruler_jojo.png">` 태그를 삽입합니다.
3. **스타일 갱신 (`style.css`)**:
   - 초상화 이미지 크기를 적절하게 제한하고, 원형으로 마스킹(`border-radius: 50%`)하여 고급스러운 테두리(border) 효과를 줍니다.
   - 초상화와 나머지 군주 상태 정보가 나란히 배치되도록 Flexbox 레이아웃을 조정합니다.

---

## [기능 구현 6] 군사(징병/훈련) 시스템 구현
### 목표
- 플레이어가 지배하는 도시의 국방력을 강화하기 위해 병력을 모으는(징병) 기능과 군대의 실전 능력을 키우는(훈련) 기능을 구현합니다.
- 내정과 유사하게 명령서를 소모하고 금과 군량을 소비하는 정밀한 군사 동작을 구현합니다.

### 작업 내역
1. **군사 및 부대 데이터 구조 확장 (`script.js`)**:
   - `cityState`에 병력(현재 병사 수: 기본 2000명), 부대 훈련도(기본 40) 데이터를 추가합니다.
2. **군사 메뉴 UI 구현 (`index.html` / `script.js` / `style.css`)**:
   - [군사] 버튼 클릭 시 메인 화면 영역(`.main-content`)이 군사 관리 화면으로 교체됩니다.
   - 군사 화면에는 현재 **[병력 수]**, **[훈련도]**가 표시되며, **[징병 (-1 명령서)]**, **[훈련 (-1 명령서)]**, **[돌아가기]** 버튼이 나타납니다.
3. **군사 행동 로직 구현 (`script.js`)**:
   - **징병**:
     - 명령서 1개, 금 200이 필요합니다.
     - 실행 시 병력이 1000명 증가하고, 징병의 여파로 훈련도가 5만큼 감소합니다. (훈련도는 0 이하로 내려가지 않음)
     - 자원이나 명령서가 부족하면 실패 경고를 보여줍니다.
   - **훈련**:
     - 명령서 1개, 군량 500이 필요합니다.
     - 실행 시 부대 훈련도가 10 ~ 20 사이로 무작위 증가하며 최대 100까지 도달할 수 있습니다.
     - 자원이나 명령서가 부족하면 실패 경고를 보여줍니다.
   - **돌아가기**:
     - 메인 로비 화면으로 복구합니다.

---

## [기능 구현 7] 인사(장수 목록/포상/등용) 시스템 구현
### 목표
- 소속 장수들의 상세 스탯과 충성도를 보여주는 장수 관리 화면을 구축합니다.
- 장수에게 금을 주어 충성도를 높이는 '포상' 기능과 새로운 인재를 영입하는 '등용' 기능을 구현합니다.
- 게임 내 소속 인물이 늘어나고 변화하는 동적 요소를 추가합니다.

### 작업 내역
1. **장수 데이터 구조 확장 (`script.js`)**:
   - `officers` 배열에 초기 충성도(`loyalty`) 값을 추가합니다 (예: 조조 100, 하후돈 95, 순욱 98).
   - 등용할 수 있는 재야 장수 리스트(`freeAgents`)를 새로 정의해 둡니다 (예: '허저', '곽가', '장료' 등).
2. **인사 메뉴 UI 구현 (`index.html` / `script.js` / `style.css`)**:
   - [인사] 버튼 클릭 시 메인 화면 영역이 인사 메뉴로 교체됩니다.
   - 소속 장수 목록(이름, 무력, 지력, 정치, 매력, 충성도)을 테이블(표) 형태로 깔끔하게 표시합니다.
   - 장수 목록 아래에는 **[포상 (-1 명령서, 금 -100)]**, **[인재 수색 (-1 명령서)]**, **[돌아가기]** 버튼이 생성됩니다.
3. **인사 행동 로직 구현 (`script.js`)**:
   - **포상**:
     - 명령서 1개, 금 100이 필요합니다.
     - 실행 시 충성도가 100 미만인 소속 장수 중 가장 충성도가 낮은 한 명을 골라 충성도를 `+10 ~ +20` 무작위 증가시킵니다 (최대 100).
     - 금이나 명령서가 모자라거나 모든 장수의 충성도가 이미 100이면 실행을 차단하고 안내 메시지를 보여줍니다.
   - **인재 수색(등용)**:
     - 명령서 1개 소모.
     - 일정한 확률(예: 50%)로 재야 장수 수색에 성공합니다.
     - 등용에 성공하면 재야 장수 중 한 명이 아군 소속(`officers`)으로 영입되며, 웰컴 축하 텍스트를 메인 화면에 띄웁니다.
     - 더 이상 등용할 재야 장수가 없으면 "더 이상 재야에 인재가 없습니다" 문구를 보여줍니다.
   - **돌아가기**:
     - 메인 로비 화면으로 복구합니다.

---

## [기능 구현 8] 자원(금/군량) 디버그 수정 기능 구현
### 목표
- 플레이어가 게임 테스트 및 난이도 조절을 위해 자금(금)과 군량 수치를 실시간으로 자유롭게 편집할 수 있는 디버그 기능을 추가합니다.
- 복잡한 UI 추가 없이, 기존 내정/군사/인사 화면에 표시된 자원 정보 옆에 수정 링크를 배치하여 직관적으로 값을 변경하도록 설계합니다.

### 작업 내역
1. **수정(편집) UI 배치 (`script.js` / `style.css`)**:
   - 내정, 군사, 인사 메뉴에서 금과 군량이 표시되는 스탯 정보창(`.city-stats`) 내부 수치 옆에 작은 연필 모양이나 `[편집]` 링크 버튼을 삽입합니다.
   - 링크 버튼의 폰트 크기나 마우스 커서 스타일 등을 지정해 줍니다 (예: `cursor: pointer; color: #ffd700; font-size: 0.8rem; margin-left: 5px;`).
2. **수정 입력창 및 값 변경 로직 구현 (`script.js`)**:
   - `[편집]` 링크 클릭 시 브라우저 기본 입력창(`prompt`)을 띄워 변경하고자 하는 수치를 입력받습니다.
   - **유효성 검사**:
     - 사용자가 숫자가 아닌 값을 입력하거나 음수를 입력하는 경우 수정을 차단하고 "올바른 숫자를 입력하세요" 알림을 띄웁니다.
     - 정상적인 정수가 입력되면 `cityState.gold` 또는 `cityState.grain` 값을 해당 숫자로 즉시 업데이트합니다.
   - 값 변경 후, 현재 열려 있는 메뉴 화면(내정, 군사, 인사)을 즉시 새로고침하여 바뀐 수치가 적용되게 만듭니다.

---

## [기능 구현 9] 중국 전역 지도(다중 지역 선택) 시스템 구현
### 목표
- 단일 도시('허창')만 관리하던 것에서 벗어나, 삼국지 주요 거점 도시들이 배치된 전술 지도를 만듭니다.
- 지도상의 거점(도시)들을 클릭하여 다른 지역의 군주 지배 현황, 병력, 자원 데이터를 실시간으로 조회하고 선택할 수 있도록 설계합니다.

### 작업 내역
1. **다중 도시 데이터 풀 구축 (`script.js`)**:
   - 기존의 단일 `cityState` 변수를 폐지하고, 여러 도시를 갖는 객체 배열 `cities` 구조로 리팩토링합니다.
   - **도시 목록**:
     - **허창** (플레이어 세력: 조조, 금 1000, 군량 5000, 병력 2000, 훈련 40, 농업/상업/치수 50)
     - **낙양** (중앙 거점 / 동탁 세력, 금 2500, 군량 12000, 병력 8000, 훈련 60, 농업/상업/치수 70)
     - **업** (북방 요충지 / 원소 세력, 금 1500, 군량 8000, 병력 4000, 훈련 50, 농업/상업/치수 60)
     - **서주** (동부 비옥지 / 도겸 세력, 금 1200, 군량 6000, 병력 3000, 훈련 45, 농업/상업/치수 55)
     - **건업** (남부 요충지 / 손책 세력, 금 1800, 군량 9000, 병력 5000, 훈련 55, 농업/상업/치수 65)
2. **지도 그래픽 UI 구현 (`index.html` / `style.css` / `script.js`)**:
   - 로비의 메인 화면 영역(`.main-content`)에 다크 판타지 디자인의 간이 중국 지도를 시각화합니다.
   - 지도는 종이 재질 느낌 또는 어두운 전술판 배경으로 스타일링하며, 각 도시는 절대 위치(`position: absolute`)를 활용해 가로/세로 비율(%)로 지도판 내에 동그라미 성곽 거점으로 배치됩니다.
3. **도시 정보 상세 팝업 및 선택 인터페이스 (`script.js` / `style.css`)**:
   - 지도상의 특정 거점 도시를 클릭하면 해당 도시의 현황(지배 세력, 자금, 식량, 국방력, 개발 상태)을 일목요연하게 팝업창이나 전용 디테일 뷰 영역에 노출합니다.
   - 플레이어가 지배하는 도시('허창')를 선택하면 기존에 구현된 내정, 군사, 인사 관리 메뉴가 활성화되어 명령을 내릴 수 있습니다.
   - 타 세력 도시를 선택한 상태에서는 내정/군사/인사 명령이 차단되며 "타 세력의 도시입니다" 문구를 띄웁니다.

---

## [기능 구현 10] 장수별 병력 배속(할당) 시스템 구현
### 목표
- 도시가 지닌 총 예비 병력을 부하 장수들에게 각각 나누어 배속할 수 있는 병력 관리 시스템을 추가합니다.
- 장수마다 병력을 데리고 다닐 수 있도록 함으로써, 향후 부대 단위 전투와 영토 방어의 기반을 마련합니다.

### 작업 내역
1. **장수 데이터 구조 확장 (`script.js`)**:
   - `officers` 배열 내 장수 객체(조조, 하후돈, 순욱 및 신규 등용 장수)에 배속 병력 수 속성(`soldiers: 0`)을 추가합니다.
2. **도시의 병사 개념 재정의 (`script.js`)**:
   - 플레이어 지배 도시(허창)의 병사 수(`cities['허창'].soldiers`)를 "장수에게 배치되지 않은 예비 병력(예비군)" 개념으로 전환합니다.
3. **인사 메뉴(장수 목록)의 UI 확장 (`script.js` / `style.css`)**:
   - 인사 화면의 장수 현황 테이블에 **[배속 병력]** 및 **[조작]** 열을 신설합니다.
   - 조작 열에 **[병력 배속]** 버튼을 추가합니다.
   - 장수 목록 하단에 현재 도시의 **[예비 병력 수]**를 명확히 보여줍니다.
4. **병력 배속 로직 구현 (`script.js`)**:
   - 특정 장수의 **[병력 배속]**을 클릭하면 브라우저 입력창(`prompt`)이 열립니다.
   - "이 장수에게 배속할 병사 수를 입력하세요 (도시 예비 병력: X명, 기존 배속: Y명):" 메시지를 출력합니다.
   - **유효성 검사**:
     - 음수나 문자가 입력되는 경우 차단합니다.
     - (기존 배속 병력 + 도시 예비 병력)을 초과한 병력을 할당하려는 경우 "예비 병력이 부족합니다" 에러를 띄웁니다.
   - 검증 성공 시:
      - 장수의 배속 병력을 입력된 수치로 갱신합니다.
      - 변경된 병력의 차이(새 병력 - 기존 병력)만큼 도시 예비 병력(`cities['허창'].soldiers`)에서 차감하거나 되돌려줍니다.
      - 변경된 수치들로 인사 메뉴 테이블 화면을 즉시 새로고침하여 보여줍니다.
      - (참고: 단순 병력 재편성은 명령서를 소모하지 않는 행위로 처리하여 자유롭게 배분하도록 편의성을 제공합니다.)

---

## [기능 구현 11] 전쟁(출병) 시스템 구현
### 목표
- 적의 영토를 직접 공격해 점령하고 소유권을 획득하여 영토를 넓혀나가는 전쟁 시스템을 만듭니다.
- 공격할 부대(장수 및 배속 병력)를 편성하여 출병을 선포하고 자동 시뮬레이션을 통해 전투의 결과를 시각적인 텍스트 로그로 보여줍니다.

### 작업 내역
1. **[출병] UI 추가 및 지도 선택 연동**:
   - `index.html` 하단 액션 메뉴에 **[출병]** 버튼을 추가하고 스타일링합니다.
   - 플레이어가 소유하지 않은 타 세력 영토를 지도에서 선택했을 때만 **[출병]** 버튼이 활성화되도록 `script.js`의 `updateActionMenuButtons()`와 `selectCity()` 함수를 제어합니다.
2. **출병 부대 편성(출전 장수 선택) 인터페이스**:
   - [출병] 클릭 시 메인 화면 영역을 출전 장수 선택 화면으로 전환합니다.
   - 플레이어가 참전시킬 장수들을 여러 명 선택할 수 있는 체크박스나 리스트를 렌더링합니다.
   - 출병 조건: 명령서 1개 소모, 아군 본거지에 군량 1,000 이상 보유, 출전 장수들의 총 배속 병력 합계가 1명 이상이어야 합니다.
3. **적 세력 가상 장수 배치**:
   - 적 세력의 도시는 소속 장수 데이터가 없으므로 전투 시 등장할 대표 장수를 동적으로 연동합니다 (동탁군: 여포, 원소군: 안량, 손책군: 태사자, 도겸군: 장패).
4. **전투 시뮬레이션 및 결과 처리**:
   - 아군의 출전 장수들 무력과 병력, 훈련도를 종합한 전투력과 적군의 주둔 병력, 훈련도, 수비 장수의 무력을 비교합니다.
   - 차례대로 병력을 무작위 난수에 따라 가감하여 깎아나가는 턴제 전투 로그를 화면에 실시간으로 렌더링하여 보여줍니다.
   - 아군 승리(적군 전멸) 시 해당 도시의 지배권(`owner`)이 '조조'로 바뀌며, 지도의 색상과 깃발이 변경됩니다. 남은 아군 병력은 해당 도시의 주둔군이 되며 장수들은 복귀합니다.
   - 아군 패배(아군 전멸) 시 참전했던 장수들의 배속 병력은 0이 되고, 본거지로 패주해 되돌아옵니다.

---

## [기능 구현 12] 저장 및 불러오기(Save & Load) 기능 구현
### 목표
- 사용자가 게임을 진행하다가 언제든 현재 상태를 브라우저에 저장하고, 나중에 이어서 플레이할 수 있도록 저장/불러오기 기능을 추가합니다.
- 데이터베이스 서버 없이도 로컬 환경에서 영구 보존이 가능하도록 브라우저 내장 저장소(localStorage)를 활용합니다.

### 작업 내역
1. **헤더 영역 [저장] 및 [로드] 버튼 배치**:
   - `index.html` 상단 헤더의 타이틀 옆에 **[저장]**, **[로드]** 미니 시스템 버튼을 추가합니다.
   - `style.css`에서 시스템 버튼이 헤더 디자인과 조화롭게 어울리도록 컴팩트한 다크/실버 톤 스타일을 적용합니다.
2. **게임 상태 직렬화 및 로컬 저장 기능 구현**:
   - `script.js`에 `saveGame()` 함수를 작성합니다.
   - 현재 플레이어 상태(`playerState`), 전체 도시 자원 및 소유주 정보(`cities`), 아군 장수 현황(`officers`), 재야 장수 리스트(`freeAgents`), 그리고 현재 선택된 도시 명(`selectedCityName`)을 하나의 데이터 꾸러미(JSON)로 묶어 `localStorage`에 저장합니다.
   - 저장 완료 시 "💾 게임이 성공적으로 저장되었습니다." 알림창을 출력합니다.
3. **게임 상태 파싱 및 실시간 화면 복원 기능 구현**:
   - `script.js`에 `loadGame()` 함수를 작성합니다.
   - `localStorage`에서 저장된 데이터 패키지를 읽어옵니다. 데이터가 없는 경우 "⚠️ 저장된 게임이 없습니다!" 경고를 띄웁니다.
   - 저장된 데이터가 존재하면 JSON 형태에서 자바스크립트 객체 형태로 변환하여 게임 데이터 상태를 전부 덮어씁니다.
   - 데이터를 덮어쓴 뒤, 상단 상태 바(`updateStatusBar()`) 및 중앙 메인 지도 화면(`showMapScreen()`)을 강제로 재렌더링하여 실시간으로 복구된 화면을 보여줍니다.
   - 불러오기 완료 시 "📂 게임을 성공적으로 불러왔습니다." 알림창을 출력합니다.

---

## [기능 구현 13] 세금 징수 및 유지비(자원 순환) 시스템 구현
### 목표
- 게임의 현실성을 높이고 자원이 유기적으로 흐르도록 매년 특정 계절(월)에 세금을 걷고, 매월 장수와 군사 유지 비용을 지출하는 시스템을 구축합니다.
- 자금 부족 시 장수 충성도 하락, 식량 부족 시 병사 탈영 등의 디버프 효과를 부여하여 자원 관리의 재미를 배가시킵니다.

### 작업 내역
1. **세금 징수 로직 구현 (매년 1월/7월)**:
   - `script.js` 내에 턴 종료(`btnNextTurn` 이벤트) 발생 시 세금을 정산하는 로직을 통합합니다.
   - **금 수입 (매년 1월)**: 플레이어 영토인 도시들의 **상업 개발도**에 비례하여 세금을 거둡니다 (`금 수입 = 상업도 * 15 + 300`).
   - **군량 수입 (매년 7월)**: 플레이어 영토인 도시들의 **농업 개발도**에 비례하여 군량을 거둡니다 (`군량 수입 = 농업도 * 60 + 1000`).
   - 세금 징수 성공 시 화면 메인에 어떤 영토에서 얼마만큼의 세금을 거뒀는지 종합 리포트 텍스트를 노출합니다.
2. **매월 유지비 및 급료 차감 로직 구현**:
   - 매달 턴이 종료될 때마다 플레이어 영토의 군사와 소속 장수들에게 유지 자원을 차감합니다.
   - **장수 급료 (금)**: 장수 1명당 매달 **20금**을 지급합니다.
     - 본진 허창의 자금이 부족하여 지급하지 못할 경우, 소속 장수 전원의 충성도(`loyalty`)가 5 하락합니다.
   - **군사 유지비 (군량)**: 아군 영토 내 모든 병사(장수 배속 병력 및 도시 예비군) 수에 비례하여 **식량**을 소모합니다 (`소모량 = 총 병력 * 0.1`).
     - 본진의 식량이 부족할 경우, 굶주린 병사들이 동요하여 매달 전체 병력의 10%가 탈영(감소)합니다.
3. **자원 소모 리포트 팝업 및 화면 동기화**:
   - 매달 턴 종료 시 지출액과 자원 부족으로 인한 부작용(탈영, 충성도 하락)을 요약하여 하단 결과 영역에 띄워주고, 상단 자원 스탯바를 갱신합니다.

---

## [기능 구현 14] 명령서 직접 수정(디버그) 기능 구현
### 목표
- 플레이어가 디버깅 및 테스트를 편하게 할 수 있도록, 상단 상태바에 노출되는 명령서(행동력)의 개수를 원하는 값으로 자유롭게 수정하는 기능을 도입합니다.
- 게임 밸런스 훼손을 방지하기 위해 최대 10개 한도로 수정 제한 조건을 추가합니다.

### 작업 내역
1. **상태바 수정 버튼 추가 (`script.js` 또는 `index.html`)**:
   - 상단 상태바의 명령서 개수 출력부 옆에 **✏️** 연필 모양 수정 버튼을 배치합니다.
   - 클릭 시 명령서 수정 전용 함수 `modifyActionPoints()`가 작동하도록 연결합니다.
2. **명령서 수정 함수 구현 (`script.js`)**:
   - `modifyActionPoints()` 함수를 생성하여 브라우저 입력창(`prompt`)으로 변경할 명령서 개수를 입력받습니다.
   - **유효성 검사**:
     - 정수가 아닌 포맷 혹은 음수 기입 시 차단합니다.
     - **10을 초과하는 숫자 기입 시** "명령서는 최대 10개까지만 설정할 수 있습니다!" 경고창을 띄우고 차단합니다.
     - 검증 성공 시 `playerState.actionPoints` 값을 갱신하고 상태바(`updateStatusBar()`)를 리렌더링해 즉각 반영합니다.

---

## [기능 구현 15] 병력 자동 균등 분배 기능 구현
### 목표
- 플레이어가 지닌 도시의 예비 병력을 소속 장수들의 머릿수에 맞추어 고르게 자동 배속시키는 편의 기능을 구현합니다.
- 장수가 많을 때 수동으로 한 명씩 병력을 할당해야 하는 번거로움을 해결합니다.

### 작업 내역
1. **인사 관리 화면에 [자동 분배] 버튼 추가 (`script.js`)**:
   - 인사 메뉴(`showPersonnelMenu()`) 내의 도시 스탯 및 자금 정보 표시 영역 아래쪽에 **[🤖 병력 자동 균등 분배]** 서브 액션 버튼을 생성합니다.
   - 버튼 클릭 시 `autoAssignSoldiers()` 함수가 호출되도록 이벤트 리스너를 연결합니다.
2. **자동 분배 비즈니스 로직 구현 (`script.js`)**:
   - `autoAssignSoldiers()` 함수를 신설합니다.
   - **배속 대상**: 소속 장수(`officers`) 전원 및 현재 도시의 예비 병력(`cities[selectedCityName].soldiers`).
   - **분배 공식**:
     - 현재 도시의 예비 병력 수량을 구합니다. 만약 예비 병력이 장수 숫자보다 작을 경우 "분배할 병력이 부족합니다" 경고 후 종료합니다.
     - `1인당 배속량 = Math.floor(도시 예비 병력 / 장수 인원수)`을 연산합니다.
     - 자투리 병력(`자투리 = 도시 예비 병력 % 장수 인원수`)을 구합니다.
     - 각 장수의 배속 병력(`off.soldiers`)에 `1인당 배속량`을 가산해 줍니다.
     - 도시의 예비 병력은 `자투리` 병력만큼만 남기고 모두 장수들에게 넘어갑니다.
     - 작업이 완료되면 인사 메뉴 화면(`showPersonnelMenu()`)을 강제로 새로고침해 즉각 수치 동기화를 보여주고, 하단 결과란(`personnel-result`)에 균등 분배 성공 알림 메시지를 렌더링합니다.

---

## [기능 구현 16] 지도 및 도시 데이터 확장 (다중 거점 추가)
### 목표
- 기존의 5개 도시 체제에서 삼국지 주요 역사적 거점 도시 3개(성도, 양양, 평원)를 추가하여 총 8개 거점의 더 크고 완성도 높은 전술 지도를 만듭니다.
- 각 도시마다 새로운 점령 세력과 수비 장수들을 다변화하여 전쟁 시스템의 전략성을 배가시킵니다.

### 작업 내역
1. **도시 데이터 모델 확장 (`script.js`)**:
   - `cities` 객체에 신규 영토 3곳의 기초 데이터를 가산합니다.
     - **성도** (서남부 익주 요충지 / 유언 세력, 금 2000, 군량 10000, 병력 5000, 훈련 50, 농업 70, 상업 60, 치수 65)
     - **양양** (남부 중앙 형주 요충지 / 유표 세력, 금 1500, 군량 7000, 병력 3000, 훈련 45, 농업 65, 상업 55, 치수 60)
     - **평원** (북동부 기주 변방 / 공손찬 세력, 금 1000, 군량 5000, 병력 2500, 훈련 40, 농업 45, 상업 40, 치수 50)
2. **지도의 노드 시각화 확장 (`script.js` / `style.css` / `index.html`)**:
   - `script.js` 내의 `showMapScreen()` 함수 내부 HTML 템플릿에 신규 거점 3곳을 전술판 내 겹치지 않는 적정 비율로 배치합니다.
     - 성도: `top: 72%, left: 12%`
     - 양양: `top: 66%, left: 38%`
     - 평원: `top: 20%, left: 60%`
   - `selectCity()` 내에서 해당 신규 거점 노드 ID들('node-chengdu', 'node-xiangyang', 'node-pingyuan')을 하이라이트 활성화 처리 목록에 연결합니다.
   - `style.css`에 신규 세력 전용 색상 테마 스타일을 정의해 깃발과 깜빡임 효과를 꾸밉니다.
     - 유언군(주황색): `.city-node.owner-liu`
     - 유표군(녹갈색): `.city-node.owner-bia`
     - 공손찬군(하늘색): `.city-node.owner-gong`
3. **수비 대장 및 난이도 매핑 (`script.js`)**:
   - `executeBattleSimulation()` 내의 적 대표 장수 목록(`defenseRulerMap`)에 신규 거점의 명장들을 바인딩하여 수비 난이도를 조율합니다.
      - 성도: 명장 **장임** (무력 84)
      - 양양: 명장 **황충** (무력 93)
      - 평원: 명장 **조운** (무력 96)

---

## [기능 구현 17] 다중 세이브 슬롯 (저장 슬롯 여러 개 지원) 기능 구현
### 목표
- 단일 저장소 구조에서 벗어나, 플레이어가 게임 상태를 3개의 다른 슬롯(슬롯 1, 슬롯 2, 슬롯 3) 중 원하는 곳을 선택해 저장하고 개별적으로 불러올 수 있게 만듭니다.
- 프롬프트 선택 창에서 각 슬롯에 어떤 연도/월 상태의 게임이 저장되어 있는지 메타 정보를 요약해 주어 플레이어의 식별 편의성을 극대화합니다.

### 작업 내역
1. **슬롯 정보 조회 헬퍼 함수 구현 (`script.js`)**:
   - `getSlotInfo(slotNum)` 함수를 신설합니다.
   - 로컬스토리지 키 `samgukgi_save_slot_X`에 보관된 데이터를 읽어와서 파싱한 후, 해당 저장본의 연도와 월, 명성 정보가 요약된 안내 텍스트를 반환합니다. 데이터가 없으면 "저장 데이터 없음"을 표시합니다.
2. **저장 로직의 슬롯 선택 인터페이스 보완 (`script.js`)**:
   - `saveGame()`을 수정하여 클릭 시 1~3번 슬롯의 상세 내역 목록이 포함된 브라우저 입력창(`prompt`)을 띄웁니다.
   - 사용자가 `1`, `2`, `3` 중 하나를 선택하면 해당 슬롯 번호 키(`samgukgi_save_slot_X`)로 게임 데이터를 인코딩하여 로컬스토리지에 저장합니다.
   - 잘못된 기입 시 예외 경고 후 취소시킵니다.
   - 저장 완료 시 슬롯 번호를 명시한 알림창을 띄웁니다.
3. **불러오기 로직의 슬롯 선택 인터페이스 보완 (`script.js`)**:
   - `loadGame()`을 수정하여 클릭 시 1~3번 슬롯의 저장 현황 목록을 입력창(`prompt`)으로 출력합니다.
   - 사용자가 슬롯 번호를 선택하면 해당 키에서 데이터를 불러와 덮어쓴 뒤 화면 상태를 리렌더링하여 복구시킵니다.
   - 선택한 슬롯에 데이터가 없는 경우 경고창을 띄우고 동작을 중단시킵니다.

---

## [기능 구현 18] 전술 지도 확대 및 거점(성) 대규모 추가 (총 11개 성곽 지원)
### 목표
- 기존의 260px 높이의 좁은 전술 지도를 380px 높이로 크게 확장하여 대형 모니터 및 모바일 전반의 시인성을 개선합니다.
- 지도가 확장되어 가용 공간이 확보된 만큼, 삼국지 유명 전쟁터이자 유서 깊은 거점 성곽 3곳(북해, 신야, 진양)을 추가하여 총 11개 거점의 대규모 판도로 구축합니다.

### 작업 내역
1. **지도 컨테이너 크기 확대 (`style.css`)**:
   - `.map-container`의 높이(`height`)를 `260px`에서 **`380px`**로 늘려 비주얼 크기를 확보하고 바둑판 전술판의 감성을 극대화합니다.
2. **신규 3개 도시 데이터베이스 등록 (`script.js`)**:
   - `cities` 풀에 다음 3개 영토의 내정 스탯과 병력을 기산합니다.
     - **북해** (산동 지역 요충지 / 공융 세력, 금 1200, 군량 6000, 병력 3000, 훈련 45, 농업 55, 상업 50, 치수 55)
     - **신야** (중남부 요충지 / 유비 세력, 금 800, 군량 4000, 병력 2000, 훈련 50, 농업 50, 상업 40, 치수 45)
     - **진양** (북서부 변방 관문 / 장양 세력, 금 1100, 군량 5500, 병력 2700, 훈련 40, 농업 45, 상업 45, 치수 50)
   - `executeBattleSimulation()` 내의 적군 장수 맵(`defenseRulerMap`)에 침공 시 출전할 명장을 등록합니다.
     - 북해: 군주 **공융** (무력 40)
     - 신야: 군신 **관우** (무력 97)
     - 진양: 군주 **장양** (무력 68)
3. **지도의 노드 시각화 및 위치 조율 (`script.js` / `style.css`)**:
   - `showMapScreen()` 템플릿 마크업 내에 신규 노드들을 추가하고, 11개 도시의 절대 좌표 % 비율을 겹치지 않게 전체적으로 미세 재배치합니다.
     - 진양 (북서): `top: 15%, left: 22%`
     - 평원 (북동): `top: 18%, left: 56%`
     - 북해 (동부): `top: 30%, left: 76%`
     - 낙양 (중서): `top: 48%, left: 24%`
     - 허창 (중부): `top: 50%, left: 48%`
     - 서주 (동부): `top: 44%, left: 68%`
     - 신야 (중남): `top: 60%, left: 30%`
     - 양양 (남중): `top: 68%, left: 42%`
     - 건업 (남동): `top: 74%, left: 70%`
     - 성도 (서남): `top: 78%, left: 10%`
   - `selectCity()` 선택 하이라이트 매칭 목록에 신규 거점 ID('node-bokhae', 'node-xinya', 'node-jinyang')를 추가합니다.
   - `style.css`에 신규 세력의 전용 색상 테마 및 광채 클래스를 정의합니다.
     - 공융군 (청갈색 테마): `.city-node.owner-kong`
     - 유비군 (녹색 테마): `.city-node.owner-liu-bei`
     - 장양군 (진회색 테마): `.city-node.owner-zhang`

---

## [기능 구현 19] 타 세력 인재 등용(빼오기) 및 명성 비례 확률 시스템 구현
### 목표
- 플레이어가 타 세력에 소속된 명장들(여포, 관우 등)을 금과 명령서를 사용하여 아군으로 영입(등용)할 수 있는 기능을 추가합니다.
- 영입 성공 확률은 군주의 명성 수치에 비례하여 역동적으로 증가하게 설계하여 명성의 중요성을 높입니다.

### 작업 내역
1. **타 세력 장수 데이터 정의 (`script.js`)**:
   - 등용 대상이 될 타 세력의 명장 목록 `enemyOfficers` 배열을 새롭게 생성합니다.
     (예: '여포', '관우', '장비', '조운', '황충', '태사자', '장임', '안량', '장패' 등)
2. **인사 메뉴 UI 내 버튼 추가 (`script.js` / `style.css`)**:
   - [인사] 메뉴 화면(`showPersonnelMenu()`) 내에 **[타 세력 등용 (-1 명령서, 금 -300)]** 버튼을 추가합니다.
3. **명성 비례 성공 확률 공식 및 등용 로직 구현 (`script.js`)**:
   - `poachOfficer()` 함수를 신설합니다.
   - **비용**: 명령서 1개, 금 300 소모 (선택된 도시의 금에서 차감).
   - **확률 계산**:
     - 기본 성공 확률: 20%
     - 추가 성공 확률: 플레이어 명성 / 10 (%)
     - 총 성공 확률 = 기본 20% + (명성 / 10)% (최대 90% 제한)
     - 예: 명성이 150이면 20% + 15% = 35% 확률로 등용 성공. 명성이 500이면 20% + 50% = 70% 확률로 등용 성공.
   - **결과 처리**:
     - 성공 시 `enemyOfficers` 풀에서 무작위로 한 명의 장수를 꺼내어 플레이어의 소속 장수 `officers`에 추가합니다.
     - 배반하여 넘어온 장수이므로 초기 충성도는 낮은 값(60)으로 설정합니다.
     - 실패 시 "설득에 실패하여 거절당했습니다" 문구를 화면에 띄웁니다.
     - 더 이상 등용할 타 세력 장수가 없을 경우 "더 이상 등용할 수 있는 타 세력 장수가 없습니다"라는 안내를 제공합니다.

---

## [기능 구현 20] 각 성(도시)의 지리적 특징에 맞춘 성곽 이미지 매핑 및 UI 시각화
### 목표
- 단조로운 텍스트 정보 창에서 벗어나, 지도에서 성곽을 클릭할 때 해당 도시의 지리적 특징에 맞는 고유한 성곽 이미지(중원 평지성, 남부 수성, 서남 산성)를 띄워 시각적인 재미와 몰입도를 극대화합니다.

### 작업 내역
1. **성곽 이미지 리소스 확보 및 복사**:
   - 중원/북방 거성(기본형): 기존 `castle.png` 사용
   - 남부 수성(강/바다 인접 도시): 새로 생성한 `castle_water.png` 사용
   - 서남 산성(산악 지대 도시): 새로 생성한 `castle_mountain.png` 사용
2. **도시별 이미지 데이터 매핑 (`script.js`)**:
   - `cities` 데이터 객체 구조에 성곽 이미지 파일명 속성(`image`)을 추가합니다.
     - **중원 거성** (허창, 낙양, 업, 평원, 진양): `image: 'castle.png'`
     - **남부 수성** (건업, 서주, 북해): `image: 'castle_water.png'`
     - **서남 산성** (성도, 양양, 신야): `image: 'castle_mountain.png'`
3. **도시 정보 패널(city-info-panel) 내 이미지 렌더링 및 스타일 수정 (`script.js` / `style.css`)**:
   - `selectCity(cityName)` 함수에서 우측 정보 패널 HTML을 빌드할 때, 상단에 각 성의 이미지 `<img class="city-portrait" src="성곽이미지">` 태그가 삽입되도록 구조를 수정합니다.
   - `style.css`에 `.city-portrait` 클래스를 정의하고, 정보 패널 크기에 맞게 가로세로 비율 조절, 테두리 라운딩 및 부드러운 다크 그라데이션 광채 효과를 주어 세련되게 디자인합니다.

---

## [기능 구현 21] 병력 전량 회수 후 자동 균등 재분배 시스템 개선
### 목표
- 기존의 예비 병력만 추가 가산하던 자동 분배 방식에서 탈피하여, 모든 장수들의 기존 배속 병력을 전량 회수한 뒤 도시 예비군과 통합하여 총병력을 구하고, 이를 다시 소속 장수 전원에게 1/N로 균등하게 처음부터 다시 분배하는 완전 균등 분배 방식을 도입합니다.

### 작업 내역
1. **균등 분배 로직 수정 (`script.js`)**:
   - `autoAssignSoldiers()` 함수의 계산 방식을 수정합니다.
   - **동작 원리**:
     1. 현재 아군 본거지 내 장수들이 소유한 모든 병력의 합계를 구합니다. (`장수들 병력 총합`)
     2. 도시가 지닌 예비 병력과 `장수들 병력 총합`을 합산하여 아군이 보유한 `총 가용 병력`을 구합니다.
     3. 모든 장수의 배속 병력(`off.soldiers`)을 일시적으로 0명으로 초기화(회수)합니다.
     4. `1인당 배속량 = Math.floor(총 가용 병력 / 장수 인원수)`을 연산하여 장수들에게 동등하게 분배합니다.
     5. 자투리 병력(`총 가용 병력 % 장수 인원수`)을 구하여 도시 예비 병력(`cities[selectedCityName].soldiers`)으로 남겨둡니다.
2. **UI 새로고침 및 결과 출력**:
   - 기존과 동일하게 분배 완료 후 인사 메뉴(`showPersonnelMenu()`)를 새로고침하여 바뀐 병력을 시각적으로 갱신합니다.
   - 결과창(`personnel-result`)에 "총 X명의 병사를 전량 회수하여 Y명의 장수에게 Z명씩 고르게 나누었습니다"라는 리포트 텍스트를 자세히 렌더링합니다.

---

## [기능 구현 22] 상단 상태바 자원(금/군량/병력) 즉시 표시 및 관리(수정) 기능 구현
### 목표
- 플레이어가 지배하는 조조군의 대표 자원(본진 허창 기준 금, 군량, 예비 병력)을 상단 상태바(군주명 옆)에 상시 노출하여 직관적으로 확인하게 만듭니다.
- 각 자원 옆에 연필(✏️) 버튼을 제공하여, 인사/내정 메뉴를 열지 않고도 상단에서 직접 자금, 군량, 병력을 즉시 수정(관리)할 수 있는 편리한 디버그 제어 환경을 구축합니다.

### 작업 내역
1. **상단 상태바 레이아웃 구조 변경 (`index.html` / `style.css`)**:
   - `index.html` 내의 `<section class="status-bar">` 태그 내부에 **금**, **군량**, **병력** 표시용 `<div class="status-item">` 구조를 추가합니다.
   - 각각의 수치 영역 옆에 수정을 담당할 `<span class="edit-btn">✏️</span>` 태그를 배치합니다.
   - 수치가 한 줄로 미려하게 노출되도록 CSS 스타일을 조율합니다.
2. **실시간 자원 렌더링 연동 (`script.js`)**:
   - `updateStatusBar()` 함수 내부를 보완하여 본진인 '허창'의 자원 데이터(`cities['허창'].gold`, `cities['허창'].grain`, `cities['허창'].soldiers`)를 상단 상태바의 자원 노드들에 실시간 동기화합니다.
3. **상태바 자원 즉시 수정 함수 구현 (`script.js`)**:
   - `modifyStatusBarResource(type, label)` 함수를 추가합니다.
   - 해당 자원 클릭 시 `prompt`를 띄워 변경할 수치를 입력받습니다.
   - **유효성 검사**: 숫자가 아니거나 음수인 경우 수정을 차단합니다.
   - 성공 시 본진 '허창'의 데이터를 즉시 갱신하고 `updateStatusBar()`와 현재 활성화된 메뉴를 새로고침하여 수치를 완벽하게 동기화합니다.

---

## [기능 구현 23] 다중 아군 점령지 기반 출병 출발 도시 선택 시스템 구현
### 목표
- 기존의 '허창'에서만 군량을 소비하고 진군할 수 있었던 고정형 출병 시스템을 개선하여, 플레이어가 점령한 다양한 아군 도시 중 한 곳을 직접 '출발 기지'로 지정해 출병할 수 있게 고도화합니다.
- 점령지가 확장됨에 따라 최전방에 위치한 도시의 군량과 군사 훈련도를 활용해 전쟁을 벌이는 실전 전략 시뮬레이션의 깊이를 더합니다.

### 작업 내역
1. **출병 편성 UI 내 출발 도시 선택 요소 추가 (`script.js`)**:
   - `showWarMenu()` 함수 내부의 출병 편성 화면을 리팩토링합니다.
   - 플레이어가 소유한 아군 도시(owner가 '조조'인 도시들) 목록을 드롭다운 선택상자(`<select id="war-start-city">`)로 렌더링합니다.
   - 드롭다운 선택이 변경될 때(`change` 이벤트), 선택된 출발 도시의 현재 금, 군량 수치 및 적 도시의 정보가 동적으로 화면에 다시 갱신되도록 변경합니다. (별도의 화면 갱신 함수 혹은 UI 갱신 로직 추가)
2. **출병 실행 및 전투 훈련도 연동 수정 (`script.js`)**:
   - `startWarExecution()` 함수에서 출발 도시 드롭다운의 선택값(`startCityName`)을 읽어옵니다.
   - 기존에 '허창'으로 고정되어 있던 출발 본거지 자원 및 조건 검증(군량 1,000 소모 등)을 선택된 `startCityName` 도시의 자원으로 차감 및 검증하도록 수정합니다.
   - `executeBattleSimulation()`으로 호출을 보낼 때 `startCityName` 값을 넘겨주어, 전투 계산 시 아군의 훈련도 기여분을 해당 출발 도시의 훈련도(`cities[startCityName].training`)로 계산하도록 연동합니다.
3. **전투 리포트 및 정산 보완**:
   - 전투 성공 시 명성이 올라가고, 생존한 아군 병력이 적의 옛 도시로 주둔군으로 편입되는 메커니즘을 연동합니다.
   - 턴 종료 유지비 지급 정산 리포트 등의 일관성을 위해 각 도시별 자원 변동이 유기적으로 진행되도록 보완합니다.

---

## [기능 구현 24] 영토 점령 시 지도 상의 지배 세력(성주) 및 세력 색상 실시간 반영 버그 수정
### 목표
- 플레이어가 전쟁(출병)에 승리하여 적의 영토를 뺏었을 때, 지도상의 성곽 이름 아래의 성주 이름(태그) 및 깃발 색상(세력 클래스)이 '조조'로 바뀌지 않고 고정되어 있던 하드코딩 버그를 해결합니다.
- 지도를 렌더링할 때 각 도시의 실제 지배 세력(`owner`) 데이터와 실시간 연동되도록 하여, 영토 점령 상태가 시각적으로 즉각 반영되는 정확한 전황판을 구축합니다.

### 작업 내역
1. **세력명-CSS 클래스 매핑 테이블 정의 (`script.js`)**:
   - 군주 이름(조조, 동탁, 원소 등)과 이에 해당하는 CSS 클래스명(`owner-cho`, `owner-dong` 등)을 연결하는 매핑 객체 `ownerClassMap`을 전역에 정의합니다.
2. **지도 노드 렌더링 로직 동적화 리팩토링 (`script.js`)**:
   - `showMapScreen()` 함수 내부의 지도를 렌더링하는 HTML 템플릿을 수정합니다.
   - 각 성곽 노드의 클래스 및 소속 세력명 표기부를 하드코딩 대신, `cities[도시명].owner` 및 `ownerClassMap` 데이터를 활용한 템플릿 리터럴로 동적으로 생성하도록 변경합니다.
3. **영토 지배 상황 갱신 테스트**:
   - 전쟁 승리 후 지도 화면으로 복구될 때 성곽 하단의 군주 태그가 '조조'로 변하고, 깃발 색상이 조조군 고유의 색상(남색)으로 정상 갱신되어 깜빡이는지 확인합니다.

---

## [기능 구현 25] 100명 재야 장수 무작위 생성 및 타 세력 인재 영입/경쟁 시스템 구축
### 목표
- 단조롭던 장수 풀(3명 재야)을 대폭 넓히기 위해 게임 시작 시 삼국지 풍의 성씨와 이름 조합을 바탕으로 100명의 유니크한 재야 장수를 자동 생성합니다.
- 다른 군주(동탁, 원소 등 적 세력 도시)들에게도 각 도시에 소속 장수 목록을 부여하여 초기 장수들을 영입시키고, 매달 적 군주들도 재야 장수를 등용하도록 하여 인재 쟁탈전이 벌어지는 지능형 경쟁 시스템을 구현합니다.

### 작업 내역
1. **100명 무작위 재야 장수 생성기 구현 (`script.js`)**:
   - 중국 성씨 25종(유, 조, 손, 사마, 제갈, 장, 관, 마, 황, 조, 등, 강, 위, 순, 곽, 가, 동, 여, 주, 육, 감, 허, 방, 서, 한)과 이름 25종(웅, 돈, 연, 비, 우, 선, 권, 소, 포, 량, 욱, 가, 유, 녕, 충, 운, 초, 통, 후, 탁, 애, 회, 연, 백, 평)을 조합하여 중복이 없는 100명의 장수를 임의 생성하는 헬퍼 함수를 구축합니다.
   - 각 장수의 능력치(무력, 지력, 정치, 매력)는 30~95 사이의 난수로 동적 할당합니다.
2. **도시별 소속 장수 데이터 구조 정비 및 초기 배치 (`script.js`)**:
   - `cities` 객체의 각 도시마다 `officers` 소속 장수 배열을 추가합니다.
   - 게임 시작 시 각 적 도시에 어울리는 명장들을 초기 영입시킵니다 (예: 낙양에 여포, 신야에 관우/장비, 평원에 조운 등).
   - 자동 생성된 100명 중 약 30명을 무작위로 추출하여 타 세력의 각 도시에 고르게 사전 영입(분배)시켜 세력의 깊이를 채우고, 남은 70명은 재야 장수(`freeAgents`) 풀로 둡니다.
3. **적 도시 상세 정보창에 주둔 장수 목록 표시 (`script.js`)**:
   - `selectCity(cityName)` 함수에서, 선택한 적 도시에 머물고 있는 소속 장수들의 이름과 무력을 우측 상세 패널에 한눈에 보이게 렌더링합니다.
4. **턴 종료 시 적 세력의 인재 자동 영입(수색/등용) AI 구현 (`script.js`)**:
   - 매달 턴이 종료될 때, 플레이어가 지배하지 않는 적 도시(owner가 '조조'가 아닌 도시)들마다 15%의 독립 확률로 재야 장수(`freeAgents`) 중 1명을 수색해 자신의 도시 장수 목록(`cities[cityName].officers`)으로 영입하게 만들어 인재 확보 경쟁을 시뮬레이션합니다.
5. **타 세력 인재 등용(poachOfficer) 로직 정밀화 (`script.js`)**:
   - 기존의 고정형 `enemyOfficers` 배열을 폐지합니다.
   - 플레이어가 선택한 적 도시를 공격적으로 설득할 때, **해당 적 도시의 소속 장수 목록(`cities[selectedCityName].officers`)**에서 대상을 포섭하여 플레이어의 장수 풀(`officers`)로 데려오고 적 도시에서는 삭제되도록 정교하게 수정합니다.

---

## [기능 구현 26] 타 세력 등용(poachOfficer) 시스템 리팩토링 및 예외 처리 고도화
### 목표
- 기존의 존재하지 않는 `enemyOfficers` 전역 배열 대신, 실제 지도의 타 세력 도시에 주둔 중인 부하 무장을 설득해 데려오도록 시스템을 리팩토링합니다.
- 잘못된 도시 입력이나 주둔 무장이 없는 예외 상황을 완벽하게 예방하고, 거절 시 어떤 무장에게 거절당했는지 구체적인 연출을 추가합니다.

### 작업 내역
1. **타겟 도시 입력 인터페이스 구현 (`script.js`)**:
   - [인사] - [타 세력 등용] 클릭 시 `prompt`를 띄워 설득할 적의 도시명을 입력받습니다.
   - **3중 유효성 검사**:
     - 존재하지 않는 성 이름을 입력하면 경고를 보냅니다.
     - 이미 플레이어 소유인 아군 성을 입력하면 진입을 차단합니다.
     - 대상 도시에 주둔 중인 적 무장이 한 명도 없으면 실행을 방지합니다.
     - 위의 경고 발생 시 금 300과 명령서 1개가 차감되지 않고 온전히 반환됩니다.
2. **무작위 설득 지목 및 이적 처리 (`script.js`)**:
   - 대상 도시의 주둔 장수 목록(`cities[cityName].officers`) 중 1명을 무작위로 추출하여 아군 명성 비례 확률로 설득을 시도합니다.
   - **성공 시**: 적 도시의 장수 배열에서 타겟 무장을 제거하고, 아군 `officers` 풀에 추가하며 초기 충성도 60을 할당합니다.
   - **실패 시**: 지목했던 적장의 이름을 결과창에 명시(예: "낙양 성의 [여포]에게 설득을 시도했으나 거절당했습니다")하여 생동감을 불어넣습니다.

---

## [기능 구현 27] 자동 내정(runAutoDomestic) 기능 구현
### 목표
- 영토가 늘어남에 따라 도시마다 일일이 클릭하며 내정을 올려야 하는 플레이어의 피로도를 해결하기 위해, 남은 명령서를 일괄 소모하여 아군 전역 도시 중 내정이 취약한 곳을 컴퓨터가 스스로 찾아 발전시키는 스마트 매크로 시스템을 구축합니다.

### 작업 내역
1. **내정 UI 개편 (`script.js`)**:
   - 내정 관리 화면 하단에 **[🤖 자동 내정 (남은 명령서 소모)]** 버튼을 눈에 띄는 청록색으로 추가하고, 모바일 화면 대응을 위해 버튼 배치를 Grid/Flex 레이아웃으로 개편합니다.
2. **취약 지표 우선 탐색 알고리즘 구현 (`script.js`)**:
   - `runAutoDomestic()` 함수를 신설합니다. 남은 명령서가 있는 동안 루프를 돕니다.
   - 플레이어가 점령한 모든 조조군 도시를 검색하여, 농업/상업/치수 개발도 중 `100` 미만이면서 **전체 도시 통틀어 가장 수치가 낙후된 지표**를 1순위로 탐색해 5~15씩 무작위 상승시킵니다.
3. **자원 낭비 제어 및 상세 결과 리포팅 (`script.js`)**:
   - 모든 영토의 모든 내정이 이미 100을 기록했다면 명령서 소모 없이 "모든 도시 내정이 최대치입니다"를 출력하고 종료됩니다.
   - 개발에 성공할 때마다 어떤 성의 어떤 항목이 얼마나 상승했는지 녹색 요약 박스 내에 목록형 로그로 일목요연하게 리포트합니다.

---

## [기능 구현 28] 삼국지 성곽 거점 20개 대규모 추가 및 전술 지도 확장
### 목표
- 기존의 11개 도시 판도를 확장하여 요동, 촉, 형주, 강남을 아우르는 삼국지 전역 31개 도시 체제를 완성하고, 늘어난 노드들을 겹침 없이 보여주도록 지도 디자인을 대형화합니다.

### 작업 내역
1. **도시 데이터(cities) 20개 신설 및 세력 배분 (`script.js`)**:
   - 장안, 천수, 한중, 완, 수춘, 강릉, 장사, 시상, 오, 회계, 영안, 무릉, 영릉, 계양, 하비, 여남, 북평, 양평, 대군, 여강 총 20개 요충지를 데이터에 등록합니다.
   - 조조군(플레이어)에게 허창 본진 외에 시작 영토로 **'여남'** 성을 추가 지급하며, 타 세력(동탁, 손책, 유표 등)도 역사에 맞게 영토를 고르게 분배하여 강력한 대항 세력을 구축합니다.
2. **지도 노드 겹침 방지 및 크기 확장 (`style.css` / `script.js`)**:
   - 31개 거점 성곽을 수용하기 위해 전술 지도판 높이를 **380px에서 480px로 확장**합니다.
   - 31개 성곽의 top, left 백분율 좌표를 정교하게 획정하여 성곽이 겹치지 않고 정렬되게 맵 좌표계를 전면 업데이트합니다.
3. **고유 수비 무장 바인딩 (`script.js`)**:
   - `defenseRulerMap`에 신설된 성들의 대표 수비장수(곽사, 기령, 형도영 등)를 매핑해 침략 전투 시 고유 전투가 진행되도록 연동합니다.

---

## [기능 구현 29] 전술 지도 내 도시 간 진군 경로(이동선) 시각화
### 목표
- 전술 지도 위에서 각 도시 간 진군(이동)이 가능한 노선을 가시적인 선으로 연결하여 표시함으로써, 출병 전략을 한눈에 구상하도록 돕습니다.
- 웹 브라우저의 마우스 클릭을 방해하지 않는 SVG 투명 드로잉 레이어를 적용하여 비주얼과 사용성을 동시에 사로잡습니다.

### 작업 내역
1. **도시 간 진군 노선망 간선 데이터 획정 (`script.js`)**:
   - 지리적 인접도와 통행 가능 여부를 고려하여 31개 도시 간의 통행로 약 40여 개를 `roadRoutes` 배열로 정의합니다.
2. **반응형 오프셋 보정 및 SVG 드로잉 구현 (`script.js`)**:
   - `showMapScreen()` 내부에 절대 배치된 투명 `<svg>` 엘리먼트를 노드 하단 배경에 배치합니다.
   - 각 성곽 노드의 절반 픽셀 크기에 매칭되는 백분율 오프셋 보정 수식(가로 `+4.9%`, 세로 `+5.7%`)을 적용하여, 연결 점선이 성곽 원형의 정중앙에 정확하게 자석처럼 고정되도록 디자인합니다.
   - 마우스 클릭 간섭 회피를 위해 SVG 래퍼에 `pointer-events: none` 스타일을 적용하고, 성곽 노드 클릭 기능은 유지합니다.
